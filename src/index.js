import React from 'react';
import ReactDOM from 'react-dom';
import TheatreApp from './App';
import './index.css'; // Add CSS if needed

ReactDOM.render(
  <React.StrictMode>
    <TheatreApp />
  </React.StrictMode>,
  document.getElementById('root')
);
